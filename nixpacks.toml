[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "python -m pip install -r requirements-backend.txt"]

[phases.build]
cmds = ["echo 'Backend build complete'"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"

